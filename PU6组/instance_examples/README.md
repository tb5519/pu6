# Instance data templates

`instance/` is the runtime data directory and must not be managed by Git.

These files are empty structure examples only. Do not copy real production data
into this directory.
